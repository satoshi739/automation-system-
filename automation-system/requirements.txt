requests>=2.32.3
cryptography>=41.0.0
flask==3.0.3
schedule==1.2.2
python-dotenv==1.0.1
pyyaml==6.0.1
line-bot-sdk==3.21.0
anthropic>=0.28.0
# Google Drive / GA4 / Search Console
google-auth==2.29.0
google-auth-oauthlib==1.2.0
google-api-python-client==2.126.0
google-analytics-data>=0.18.0
# Stripe
stripe>=7.0.0
# Twitter/X
tweepy>=4.14.0
# 本番サーバー
gunicorn==22.0.0

# ── 動画パイプライン（video/）──────────────────────
# Google AI Studio SDK（Veo動画生成 + Gemini）
google-genai>=1.0.0
# TTS: OpenAI TTS（高品質・プライマリ）→ gTTS（無料・フォールバック）
openai>=1.0.0
gTTS>=2.5.0
# 画像処理（既存のimage_generator.pyも使用）
Pillow>=10.0.0
# ffmpegはシステムにインストール済みを使用（pip不要）
